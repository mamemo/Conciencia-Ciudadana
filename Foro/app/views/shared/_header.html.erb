 <nav id="mainNav" class="navbar navbar-default navbar-fixed-top navbar-custom">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header page-scroll">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span> Menu <i class="fa fa-bars"></i>
                </button>
                <a class="navbar-brand" href="http://localhost:3000/">Conciencia Ciudadana</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    <li class="hidden">
                        <a href="http://localhost:3000/"></a>
                    </li>
                    <li class="page-scroll">
                        <a href="http://localhost:3000/">Noticias</a>
                    </li>
                    <li class="page-scroll">
                        <a href="http://localhost:3000/temas">Foro</a>
                    </li>
                    <li class="page-scroll">
                        <a href="#acerca">Acerca de</a>
                    </li>
                    <% if !user_signed_in? %>
                    <li class="page-scroll">
                        <a href="http://localhost:3000/users/sign_in">Ingresar</a>
                        <% else %>
                            Usuario: <%= link_to current_user.email, destroy_user_session_path, :method => :delete %>
                            <br> Puntos: <%= current_user.puntos %>
                       
                    </li>
                    <% end %>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>